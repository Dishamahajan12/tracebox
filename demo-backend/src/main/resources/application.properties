spring.application.name=demo
server.port=${PORT:8081}

spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?user=postgres.zvkbmwydrikbitbbbbhj&password=Tracebox%40123&sslmode=require}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

spring.datasource.hikari.maximum-pool-size=${DB_POOL_MAX_SIZE:10}
spring.datasource.hikari.minimum-idle=${DB_POOL_MIN_IDLE:2}
spring.datasource.hikari.idle-timeout=${DB_POOL_IDLE_TIMEOUT_MS:600000}
spring.datasource.hikari.max-lifetime=${DB_POOL_MAX_LIFETIME_MS:1800000}
spring.datasource.hikari.connection-timeout=${DB_POOL_CONNECTION_TIMEOUT_MS:30000}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:traceboxdisha12@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gsyanaybzgctnjpg}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.security.jwt.secret=${JWT_SECRET:tracebox-tracebox-tracebox-tracebox-secret-key-2026}
app.security.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
app.security.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

app.ai.duplicate-check.enabled=${AI_DUPLICATE_CHECK_ENABLED:true}
app.ai.duplicate-check.base-url=${AI_DUPLICATE_CHECK_BASE_URL:http://localhost:8001}
app.ai.duplicate-check.threshold=${AI_DUPLICATE_CHECK_THRESHOLD:0.78}
app.ai.duplicate-check.candidate-limit=${AI_DUPLICATE_CHECK_CANDIDATE_LIMIT:200}
